package razie.base;

/** these types MUST be supported by forms for capture, not necessarily by displays */
public enum AttrType {
   STRING, MEMO, SCRIPT, INT, FLOAT, DATE, DEFAULT, ENUM /** ENUM not supported yet */
}